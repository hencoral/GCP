function validar_envia(){ 
    //valido el nombre 
  
	
	if (a.concepto.value.length ==0) {
	alert("Escriba por lo menos 4 caracteres en el campo");
	a.concepto.focus();
	return false;
	
	}
	return true;
	    
} 
